DECLARE
	N RANDOM_NUM.RAND_NO%TYPE;
	C NUMBER(2):=0;
	X NUMBER(20);
BEGIN
	WHILE C < 10
	LOOP
		SELECT DBMS_RANDOM.RANDOM() INTO X FROM DUAL;
		N:=X;
		C:=C+1;
		INSERT INTO RANDOM_NUM VALUES(N);
	END LOOP;
END;
/
	
	
