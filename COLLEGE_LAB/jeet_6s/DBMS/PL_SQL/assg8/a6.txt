DECLARE
	A SPHERE.AREA%TYPE;
	V SPHERE.VOLUME%TYPE;
	R SPHERE.RAD%TYPE;
	C NUMBER(20):=0;
BEGIN
	WHILE C < 20
	LOOP
		R:=C;
		A:=(4*22*R*R)/7;
		V:=(4*22*R*R*R)/(7*3);
		C:=C+1;
		INSERT INTO SPHERE VALUES(R,A,V);
	END LOOP;
END;
/
	
	
